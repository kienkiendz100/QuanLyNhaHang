version: '3.8'

services:
  app:
    build: .
    container_name: quanlynhahang
    ports:
      - "8080:8080"
    depends_on:
      - redis

  redis:
    image: redis:7
    container_name: redis
    ports:
      - "6379:6379"